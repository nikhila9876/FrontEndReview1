# Server Configuration
PORT=5000
CLIENT_URL=http://localhost:5173

# Database Configuration (Connect when ready)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/hackathon_db?retryWrites=true&w=majority

# JWT Authentication
JWT_SECRET=replace_this_with_a_secure_random_secret_in_production
JWT_EXPIRES_IN=7d
